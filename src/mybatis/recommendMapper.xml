<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="recommend">
	<insert id="add" parameterType="vo.RecommendVo">
		<!-- 조건문 -->
	</insert>
	<update id="updateByRecommendNum" parameterType="java.lang.String">
		update board set recommend_num=recommend_num+1 where board_num=#{board_num}
	</update>
	<delete id="remove" parameterType="java.lang.String">
		<!-- 조건문 -->
	</delete>
	<select id="getlist" resultType="vo.RecommendVo">
		<!-- 조건문 -->
	</select>
	<select id="find" parameterType="String" resultType="vo.RecommendVo">
		<!-- 조건문 -->
	</select>
	<select id="getInfo" parameterType="String" resultType="hashmap">
		<!-- 조건문 -->
	</select>
</mapper>